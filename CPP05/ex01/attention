les 2 var const int c'est pour un grade "minimum" a avoir pour le signer/executer
ces 2 vars suivent les memes regles que les bureaucrats (1-150, "reversed")
	donc throw Form::GradeTooHighException et Form::GradeTooLowException

getters pour tous les attribus

fonction beSigned(Bureaucrat)
	si grade ok, signed = 1

overload <<
	print toutes les infos du forms

AUTRES mais important

fonction signForm(Form ?)
	si le form est signe, print <bureaucrat> signed <form>
	else, print <bureaucrat couldn't sign <form> because ...

un main plein de tests comme on adore
!!valgrind
